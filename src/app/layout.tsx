import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MCP Client - Model Context Protocol Explorer",
  description: "A web-based client for exploring Model Context Protocol (MCP) servers. Connect to MCP servers to discover and interact with their tools, resources, and prompts.",
  keywords: ["MCP", "Model Context Protocol", "AI", "LLM", "Tools", "Resources", "Prompts"],
  authors: [{ name: "MCP Client" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
